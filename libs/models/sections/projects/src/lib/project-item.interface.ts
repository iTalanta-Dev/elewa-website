/* Write the interface to determine the type of the data to be input into the Card Component */

export interface ProjectItem {
  title: string;
  description: string;
  tag: string;
  imgSrc: string;
}
